@model IEnumerable<testtt.Repository.Models.Motoristum>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NomeCompleto)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CategoriaCarteira)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NomeCompleto)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CategoriaCarteira)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Cpf">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Cpf">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Cpf">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
